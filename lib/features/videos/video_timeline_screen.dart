import 'package:flutter/material.dart';

class VideoTimeLineScreen extends StatelessWidget {
  const VideoTimeLineScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
